BoxDecoration -> BoxShadow()    

Transfor.translate()

AnimatedSwitcher()

Opacity, Visibility, Offstage  // Hide the widget

AbsorbPointer, IgnorePointer   // To disable subtree or Widget

BoxPainter()
Canvas()

RichText()
PageView()

RotatedBox(), Transform()

Material 

LayerLink(), CompositeTransformTarget(), CompositeTransformFollower()
FocusNode()

BackdropFilter()   // manipulate background of current child
ListBody()

ConstrainedBox(), LayoutBuilder(), FractionallySizedBox()

SvgPicture.asset(), Image.asset(), AspectRatio()

Link(), MouseRegion()

IntrinsicHeight(), IntrinsicWidth()

ExpansionTile(), PageStorage()

TabPageSelector()

AnnotatedRegion

ShaderMask()

HookBuilder(), FutureBuilder(), StreamBuilder()

NestedScrollView, FlexibleSpaceBar()

TabBarView(), DefaultTabController()


---------

RepaintBoundary(), RepaintBoundary()   // helps to get rid of unnecessary re-paint of neighbors // layers